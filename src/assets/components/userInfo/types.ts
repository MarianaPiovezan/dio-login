import React from "react";
import { Url } from "url";

export interface IUserInfo {
    name: string;
    image?: string;
    percentual: number; 
}

export interface IUserInfoStyle {
    percentual: number;
}